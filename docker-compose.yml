version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: netflix_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: netflix
    volumes:
      - backend_data:/var/lib/postgresql/data
    networks:
      - backend_net

  node:
    image: node:latest
    container_name: netflix_rest
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    environment:
      PORT: 3000

      CORS_ORIGIN: http://localhost:5173

      POSTGRES_DB: netflix
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432

      JWT_SECRET: stinger-uncapped-negligent-dish-trimness-raft
      JWT_EXPIRES_IN: 3600

      ADMIN_USERNAME: admin
      ADMIN_EMAIL: admin@localhost
      ADMIN_PASSWORD: admin
    depends_on:
      - postgres
    networks:
      - backend_net
    ports:
      - "3000:3000"
    command:
      sh -c "npm install && npm run migrator:up && npm run dev"

volumes:
  backend_data:

networks:
  backend_net:
